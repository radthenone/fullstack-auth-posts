import { makeStyles } from '@material-ui/core/styles';

const footerStyles = makeStyles({
  base: {
    color: 'black',
    minHeight: '25px',
    height: '50px',
    backgroundColor: 'white',
  },
});

export { footerStyles };
